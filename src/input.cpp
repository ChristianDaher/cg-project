#include "input.h"
#include <GL/glut.h>

// Define a function to handle keyboard input
void handleKeyboard(unsigned char key, int x, int y)
{
    // Pass the key to the Game class to handle input
    // For example, you might call game.handleInput(key);
}

// Define a function to handle special key input
void handleSpecialKeys(int key, int x, int y)
{
    // Pass the special key to the Game class to handle input
    // For example, you might call game.handleInput(key);
}